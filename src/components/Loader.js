import React from "react";
import { PropagateLoader } from "react-spinners";

const Loader = () => {
  return <PropagateLoader className="loader_position" color="#8f78fb" size={30} />;
};

export default Loader;
